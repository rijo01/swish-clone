export default function BankIDLogo() {
  // Compact stylized "BankID" mark, original artwork.
  return (
    <span className="inline-flex items-center bg-white text-[#1A3A8A] rounded px-1.5 py-0.5 text-[10px] font-extrabold tracking-tight">
      BankID
    </span>
  );
}
